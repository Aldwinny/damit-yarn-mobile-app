import { View, Text } from "react-native";
import React from "react";

const ItemEditableCard = ({ item, controllers }) => {
  return (
    <View>
      <Text>ItemEditableCard</Text>
    </View>
  );
};

export default ItemEditableCard;
